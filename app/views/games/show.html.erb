<h1><%=@game.name %></h1>

<p><%=@game.summary %></p>

<ul>
  <li>Rating: <%=@game.rating %></li>
  <li>Release Date: <%=@game.release_date %></li>
  <li>Platform(s): <%@game.platforms.each do |platform| %>
    <%=platform.name %>,
    <% end %></li>
  <li>Genre(s): <%@game.genres.each do |genre| %>
      <%=genre.name %>,
    <% end %></li>
</ul>

<%= link_to "Go back to Games", games_path %>
<%=link_to "Delete this Game", game_path(@game), method: :delete, data: { confirm: "Really delete the game?" } %>